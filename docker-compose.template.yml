services:
  # postgres — DATABASE_PORT in ports is rewritten by scripts/initialize-workspace.sh
  postgres:
    image: postgres:16
    ports:
      - "DATABASE_PORT:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
