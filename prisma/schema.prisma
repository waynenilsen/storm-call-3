generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model TestItem {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
}
